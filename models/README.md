# Shared Model Pointer Directory

The remote workspace used this directory for lightweight model pointers or symlinks, for example `models/Wan2.1-T2V-1.3B` pointing at `StereoPilot/ckpt/Wan2.1-T2V-1.3B`.

Do not commit real model weights here.
