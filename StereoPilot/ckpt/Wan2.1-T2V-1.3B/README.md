# Wan2.1-T2V-1.3B Placeholder

Place the Wan2.1 T2V 1.3B base checkpoint files in this directory, or update the `ckpt_path` in the training TOML to your external checkpoint location.

Expected use:

```toml
[model]
ckpt_path = "/path/to/workspace/StereoPilot/ckpt/Wan2.1-T2V-1.3B"
```

Checkpoint files are intentionally not tracked by Git.
